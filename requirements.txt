Flask
Flask-Login
Flask-SQLAlchemy
Flask-Migrate
Flask-Caching
Flask-Limiter
alembic
websocket-client
Werkzeug
plexapi
requests
python-dotenv
waitress
Flask-Babel
APScheduler
efipay
mercadopago
tzlocal
Flask-SocketIO
dnspython>=2.0.0
greenlet>=3.0.3
# Não é importada diretamente pelo painel: entra como dependência de base do
# stack TLS. Fica fixada para o build ser reproduzível — reveja de tempos a
# tempos, é uma biblioteca de criptografia.
cryptography==50.0.1
gevent
gevent-websocket
# ⚠️ O `>=` é preciso: o `CMD` do Dockerfile usa `--no-control-socket`, uma flag
# que só existe a partir da 25.1.0. Numa versão anterior o gunicorn não a
# reconhece e RECUSA-SE A ARRANCAR — o contentor morre no arranque, não é um
# aviso. Mudar uma coisa obriga a mudar a outra.
gunicorn>=25.1.0
pydantic
cachelib
bleach==6.1.0
filelock
Pillow
pyTelegramBotAPI
